/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CtrlEnterBreakLineDirective } from './ctrl-enter-break-line.directive';

describe('CtrlEnterBreakLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlEnterBreakLineDirective();
    expect(directive).toBeTruthy();
  });
});
